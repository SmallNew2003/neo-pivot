## ADDED Requirements

### Requirement: 核心底座与平台适配解耦
系统 SHALL 采用“核心底座 + 适配层 + 多平台可选”的分层方式，使核心底座对外能力保持稳定语义，平台差异在适配层内被吸收。

#### Scenario: 新增平台接入不修改核心语义
- **WHEN** 需要新增一个平台接入（例如 Dify 或 n8n）
- **THEN** 平台侧差异通过新增/调整适配层完成且核心底座 API 语义保持不变

