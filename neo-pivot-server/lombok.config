# Neo Pivot 服务端 Lombok 配置
#
# 说明：
# 1) 本项目统一采用“构造函数注入”，并通过 Lombok 生成构造函数（@RequiredArgsConstructor）。
# 2) 当字段上需要 @Qualifier/@Value 等注解时，要求注解可以自动复制到构造函数参数上，
#    避免为了参数注解而退回到手写构造函数。
#
# 作者：Jelvin

config.stopBubbling = true

# 允许将字段注解复制到 Lombok 生成的构造函数参数上
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Value
